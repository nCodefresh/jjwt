version: '1.0'

steps:

  build-jjwt:
    type: build
    description: jjwt
    image-name: dleblanc/jjwt-test
    dockerfile: Dockerfile
    tag: latest
  Unit-tests:
    image: ${{build-jjwt}}
    commands:
        - echo $(date)
        - mvn dependency:go-offline

