version: '1.0'

steps:

  build-jjwt:
    type: build
    description: jjwt
    image-name: dleblanc/jjwt-test
    dockerfile: Dockerfile
    tag: latest
  Unit-tests:
    image: ${{build-jjwt}}
    working-directory: ${{initial-clone}}
    fail-fast: false
    commands:
        - echo $(date)
        - env
        - cat ./foo.txt; true
        - echo foo > foo.txt
        - echo $PWD
  validateFoo:
     image : ${{build-jjwt}}
     working-directory: ${{initial-clone}}
     commands:
         - ls
