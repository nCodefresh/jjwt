version: '1.0'

steps:

  build-jjwt:
    type: build
    description: jjwt
    image-name: dleblanc/jjwt-test
    dockerfile: Dockerfile
    tag: latest
  Unit-tests:
    image: ${{build-jjwt}}
    commands:
        - echo $(date)
        - env
        - echo The CF volume is here: $CF_VOL
    environment:
        - CF_VOL=${{CF_VOLUME}}

